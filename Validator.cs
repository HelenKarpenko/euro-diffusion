////using System;
////using System.Collections.Generic;
////using System.Text;

////namespace EuroDiffusion
////{
////	class Validator
////	{
////		private const int _maxCountriesAmount = 20;
////		private const int _minCountriesAmount = 1;

//var namePattern = "[A-Z][a-z]{1,24}$";
//			if(!Regex.IsMatch(args[0], namePattern))
//				throw new ArgumentException($"Error at line {line}. Invalid country name.");

//			for (var i = 1; i< 5; i++)
//			{ 
//				var coordinate
//				if (args[i] <= 0 || args[i] >=)
//			}
////}
////}
